# docker部署项目

## Docker部署
> docker方式需要自己准备数据库和缓存中间件，然后讲参数以环境变量的方式添加到启动命令上

```shell
```

## Docker-Compose部署

### 使用外部数据库和缓存
> 如果使用自己已有的数据库和缓存，那么只需要修改配置文件即可

#### PostgreSQL版本

```shell
```

#### MySQL版本

```shell
```

### 使用自带数据库和缓存
> 如果想一键启动项目, 可以使用这个

```shell
```

#### MySQL版本

```shell
```

